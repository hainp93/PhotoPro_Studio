# PhotoPro Studio — Requirements

# Core
numpy>=1.24
opencv-python>=4.8
pillow>=10.0
tqdm
requests
rawpy

# UI
customtkinter>=5.2
darkdetect

# AI (optional but recommended — cần cho Face Restore + AI Sharpen)
# Torch: cài manual theo GPU:
#   RTX 5090 (CUDA 12.8): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   RTX 30/40 (CUDA 12.1): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   CPU only:              pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

basicsr==1.4.2
realesrgan
facelib

# CodeFormer: cài từ GitHub
#   git clone https://github.com/sczhou/CodeFormer.git
#   cd CodeFormer && pip install -e . --no-deps
ultralytics
